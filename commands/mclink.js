exports.run = async (client, message, args) => {
  // const fs = require('fs');

  // const base = {
  //   player: []
  // };

  // base.player.push({ discord: `${message.author.id}`, username: `${args[0]}` });

  // let data = JSON.stringify(base);

  // fs.writeFileSync('minecraft.json', data);
  message.reply('WIP');
};
