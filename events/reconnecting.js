exports.run = (client, member) => {
    var config = require('../config.json');
    var colors = require('colors');
    console.log("\n");
    console.log("[RECONNECT] Attempting to reconnect to WebSocket".bold.red);
    }
    